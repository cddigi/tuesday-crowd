<configuration>
    <system.webServer>
      <rewrite>
        <rules>
          <rule name="Rewrite to google.com">
            <match url="^(.*)$" />
            <action type="Redirect" url="https://www.google.com" />
          </rule>
        </rules>
      </rewrite>
    </system.webServer>
</configuration>
